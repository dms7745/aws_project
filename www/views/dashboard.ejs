<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>대시보드 - 포트폴리오 사이트 관리</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <h2>포트폴리오 사이트 관리</h2>
            <div class="nav-right">
                <span>관리자 (<%= user.username %>)</span>
                <span class="role-badge role-<%= user.role.toLowerCase() %>"><%= user.role %></span>
                <a href="/logout" class="btn-logout">로그아웃</a>
            </div>
        </div>
    </nav>
    
    <div class="container">
        <div class="welcome-section">
            <h1>환영합니다! 👋</h1>
            <p>마지막 로그인: <%= new Date().toLocaleString('ko-KR') %></p>
        </div>
        
        <div class="menu-section">
            <h2>메뉴</h2>
            <div class="menu-buttons">
                <a href="/" class="menu-btn menu-btn-primary">
                    🏠 메인 페이지
                </a>
                <a href="/admin" class="menu-btn menu-btn-secondary">
                    📊 관리자 페이지
                </a>
                <a href="http://localhost/phpmyadmin" class="menu-btn menu-btn-info" target="_blank">
                    🗄️ phpMyAdmin
                </a>
            </div>
        </div>
        
        <div class="stats-section">
            <div class="stat-card">
                <h3>📊 사용자 수</h3>
                <p class="stat-number"><%= stats.totalUsers %></p>
            </div>
            <div class="stat-card">
                <h3>📝 활성 사용자</h3>
                <p class="stat-number"><%= stats.activeUsers %></p>
            </div>
            <div class="stat-card">
                <h3>👑 관리자</h3>
                <p class="stat-number"><%= stats.adminCount %></p>
            </div>
        </div>
        
        <div class="users-section">
            <h2>사용자 관리</h2>
            <div class="table-container">
                <table class="users-table">
                    <thead>
                        <tr>
                            <th>ID</th>
                            <th>사용자명</th>
                            <th>이메일</th>
                            <th>이름</th>
                            <th>권한</th>
                            <th>상태</th>
                            <th>마지막 로그인</th>
                            <th>생성일</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% users.forEach(function(userItem) { %>
                        <tr>
                            <td><%= userItem.id %></td>
                            <td><%= userItem.username %></td>
                            <td><%= userItem.email %></td>
                            <td><%= userItem.name || '-' %></td>
                            <td>
                                <span class="role-badge role-<%= userItem.role.toLowerCase() %>">
                                    <%= userItem.role %>
                                </span>
                            </td>
                            <td>
                                <span class="status-badge <%= userItem.status === '활성' ? 'status-active' : 'status-inactive' %>">
                                    <%= userItem.status %>
                                </span>
                            </td>
                            <td><%= userItem.last_login ? new Date(userItem.last_login).toLocaleString('ko-KR') : '-' %></td>
                            <td><%= new Date(userItem.created_at).toLocaleDateString('ko-KR') %></td>
                        </tr>
                        <% }); %>
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</body>
</html>
